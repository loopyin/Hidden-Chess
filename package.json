{
  "name": "shadow-chess",
  "version": "1.5.4",
  "description": "Hidden Chess WebSocket server",
  "main": "server.py",
  "scripts": {
    "build": "echo 'Building Hidden Chess...'",
    "start": "python3 -m pip install pygbag --break-system-packages && python3 -m pygbag --bind 0.0.0.0 --port 3000 .",
    "dev": "python3 -m pip install pygbag --break-system-packages && python3 -m pygbag --bind 0.0.0.0 --port 3000 .",
    "lint": "python3 -m compileall ."
  },
  "dependencies": {
    "requests": "^0.3.0"
  }
}
